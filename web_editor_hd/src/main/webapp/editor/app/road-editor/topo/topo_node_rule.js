/**
 * Created by wt on 2015/8/3.
 */
iD.topo.NodeRule=function()
{

  /*  topoSaat={};
    topoSaat.roadMerge(graph,sWayId,eWayId,nWayId,rel)
    {
        member=memberByRole(iD.data.RoleType.FROAD_ID);
        if(member&&(member.id==sWayId.id||member.id==eWayId.id))
        {
            member.id=nWayId.id;
        }
        member=memberByRole(iD.data.RoleType.TROAD_ID);
        if(member&&(member.id==sWayId.id||member.id==eWayId.id))
        {
            member.id=nWayId.id;
        }
        //return graph.replace(rel);
    }*/
}